<%= form_with(model:@board, local:true, class: "ui form") do |f| %>
  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name, required: true, placeholder: "Task Name", autofocus: true %>
  </div>
  <%= f.submit nil, class: "ui blue button inverted" %>
<% end %>